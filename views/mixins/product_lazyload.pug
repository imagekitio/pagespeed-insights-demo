mixin product_lazyload(product)

  article.tm-product-card
    
    //- Media
    div.tm-product-card-media
      div.tm-ratio.tm-ratio-4-3
        a.tm-media-box(href= product.href)

          //- Image
          figure.tm-media-box-wrap
            if product.image
              img(loading="lazy" src= product.image.small srcset = product.image.autoOptimized + "?tr=w-200,h-200 200w,"
                                            + product.image.autoOptimized + "?tr=w-360,h-360 360w,"
                                            + product.image.autoOptimized + "?tr=w-600,h-600 600w,"
                                            + product.image.autoOptimized + "?tr=w-800,h-800 800w"
                                        sizes = '(max-width: 480px) 90px, 300px')
            else
              +icon("image", "3")(class="uk-text-muted")

    //- Body
    div.tm-product-card-body

      //- Info
      div.tm-product-card-info

        //- Type
        div.uk-text-meta.uk-margin-xsmall-bottom= product.type

        //- Title
        h3.tm-product-card-title
          a.uk-link-heading(href= product.href)= product.name